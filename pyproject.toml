[project]
name = "file-service"
version = "0.1.0"
description = "A file service for carrying out CRUD operations on files."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "uvicorn>=0.35.0",
]
